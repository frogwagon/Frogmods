# Extracted game source, kept locally for reference while patching the
# bundle - it's Pelican Party's code, not ours to publish.
research/

# Scratch test harnesses, not part of any mod.
st.html
*.scratch.*

# Work in progress - not ready to publish yet.
narrow-one-target-practice.user.js

# OS / editor noise
.DS_Store
Thumbs.db

# wrangler's own local cache - has account id/email in it, not ours to publish.
warband-server/.wrangler/
